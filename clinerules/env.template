# ==============================================================================
# ⚠️ 此文件已迁移 — 不再作为"单一真源"
#
# Canonical 版本 → animeidea-debug/infra-template:
#   https://github.com/animeidea-debug/infra-template/blob/main/env.template
#
# 此文件保留仅用于兼容性参考。
# 新项目请从 infra-template 获取最新版本。
#
# 用法：从 infra-template 复制并修改
#   cp env.template env.local
#   vim env.local
# ==============================================================================

# 跨项目共享 NAS 配置 → 在 ~/.nas-env 中设置（所有项目自动读取）：
# 密码 → 通过 macOS Keychain 管理