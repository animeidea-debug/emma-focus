# Emma Focus — 项目级 Cline 规则
# 此文件在 git 中跟踪，克隆到任何机器后自动生效

## 部署流程
每次修改 HTML/脚本后，自动执行（无需询问）：
1. `clasp push`           # GAS 后端部署
2. `cd deploy && sh deploy.sh`  # NAS 同步（路径已迁移）或 `sh deploy/deploy.sh`

## 凭证管理
所有密码通过 macOS Keychain 读取，不在任何文件中明文存储：
- SMB: `security find-internet-password -s 192.168.6.108 -a 13918962622 -w`
- WebDAV: `security find-generic-password -s "emma-webdav" -a "garychen" -w`
- 另一台电脑首次使用前，需要先在 Keychain 中存入对应凭证

## 跨机器协作
- 另一台电脑 `git commit` 后，这边 `git pull` 拿到改动
- 这边改动后自动 `clasp push + sh deploy.sh`
- 项目状态见 `progress.md`

## Windows 首次设置
1. 安装 [Git for Windows](https://git-scm.com/)（自带 Git Bash）
2. 安装 [rclone for Windows](https://rclone.org/downloads/)
3. `git clone` 仓库后，设置密码：
   ```powershell
   setx WEBDAV_PASS "Momoco198399"
   ```
4. 打开 Git Bash，运行 `sh deploy.sh`
